<phone:PhoneApplicationPage 
    x:Class="DiversityPhone.Home"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ctrl="clr-namespace:DiversityPhone.Controls"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait" Orientation="Portrait"
    mc:Ignorable="d" d:DesignHeight="696" d:DesignWidth="480"
    shell:SystemTray.IsVisible="True"
    shell:SystemTray.ProgressIndicator="{StaticResource Progress}"
	d:DataContext="{d:DesignData /SampleData/HomeVMSampleData.xaml}"
    DataContext="{Binding Source={StaticResource Locator}, Path=Home}"
    Loaded="PhoneApplicationPage_Loaded" >

    <!--LayoutRoot is the root grid where all page content is placed-->
    <Grid x:Name="LayoutRoot" Background="Transparent">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!--TitlePanel contains the name of the application and page title-->
        <Grid x:Name="TitlePanel" Grid.Row="0" Margin="12,17,0,28">      
            <TextBlock x:Name="ApplicationTitle" Text="DIVERSITYMOBILE" Style="{StaticResource PhoneTextNormalStyle}" Width="263" HorizontalAlignment="Left" />
            <Image Height="120" Margin="0,0,26,0" Name="imageApplication" Width="140" Source="/Images/ApplicationIcon.png" Stretch="Fill" HorizontalAlignment="Right" VerticalAlignment="Bottom" />
            <TextBlock x:Name="PageTitle" Text="home" Margin="55,29,193,0" Style="{StaticResource PhoneTextTitle1Style}" Width="220" Height="91" />            
        </Grid>

        <ScrollViewer x:Name="ContentPanel" Grid.Row="1" ScrollViewer.HorizontalScrollBarVisibility="Disabled">            
            <StackPanel Margin="12,0,0,0">
                <ctrl:CommandNotAvailableLabel Text="{Binding Resources.Home_Info_CantOpenNewSeries, Source={StaticResource LocalizedStrings}}" Command="{Binding Add}"/>
                <ContentControl ContentTemplate="{StaticResource FieldDataItemTemplate}" 
                                Content="{Binding NoEventSeries}" HorizontalAlignment="Left"/>
                <ListBox ItemsSource="{Binding SeriesList}" ItemTemplate="{StaticResource FieldDataItemTemplate}" Height="449" />
            </StackPanel>
        </ScrollViewer>
    </Grid>    
    </Grid>


    <phone:PhoneApplicationPage.ApplicationBar>
        <shell:ApplicationBar IsVisible="True" IsMenuEnabled="False">
                  
        </shell:ApplicationBar> 
        
    </phone:PhoneApplicationPage.ApplicationBar>

</phone:PhoneApplicationPage>

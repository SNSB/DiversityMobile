<phone:PhoneApplicationPage 
    x:Class="DiversityPhone.EditIU"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:toolkit="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls.Toolkit"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait" Orientation="Portrait"
    mc:Ignorable="d" d:DesignHeight="696" d:DesignWidth="480"
    shell:SystemTray.IsVisible="True"
    DataContext="{Binding Source={StaticResource Locator}, Path=EditIU}">

    <!--LayoutRoot is the root grid where all page content is placed-->
    <Grid x:Name="LayoutRoot" Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!--TitlePanel contains the name of the application and page title-->
        <StackPanel x:Name="TitlePanel" Grid.Row="0" Margin="12,17,0,28">            
            <TextBlock x:Name="PageTitle" Text="identification" Margin="9,-7,0,0" Style="{StaticResource PhoneTextTitle1Style}"/>
        </StackPanel>

        <!--ContentPanel - place additional content here-->
        <StackPanel x:Name="ContentPanel" Grid.Row="1" Margin="12,0,12,0">                    
           
            <toolkit:ListPicker ItemsSource="{Binding TaxonomicGroups}" 
                                SelectedItem="{Binding SelectedTaxGroup, Mode=TwoWay}"
                                ItemTemplate="{StaticResource TermListItemTemplate}"
                                Name="_taxonListPicker"
                                FullModeItemTemplate="{StaticResource TermListPickerItemTemplate}"
                                Header="Taxonomic Group"
                                FullModeHeader="Taxonomic Group"/>
            
            <TextBlock Text="Genus"/>
            <TextBox Text="{Binding Genus, Mode=TwoWay}" />

            <TextBlock Text="Species"/>
            <TextBox Text="{Binding Species, Mode=TwoWay}" />
                        
            <ListBox ItemsSource="{Binding IdentificationCandidates}" />

            <StackPanel Visibility="{Binding IsToplevel, Converter={StaticResource BoolToVis}, ConverterParameter=True}">
                <TextBlock Text="Relationship Type"/>
                <TextBox Text="{Binding RelationshipType, Mode=TwoWay}" />
            </StackPanel>
            
            <CheckBox IsChecked="{Binding OnlyObserved}" Content="Only Observed" Visibility="{Binding IsNoObservation}"/>
            
            <ListBox ItemsSource="{Binding Circumstances}" SelectedItem="{Binding Circumstance, Mode=TwoWay}"/>
        </StackPanel>
    </Grid>
 
   <phone:PhoneApplicationPage.ApplicationBar>
        <shell:ApplicationBar IsVisible="True" IsMenuEnabled="False">
            <shell:ApplicationBarIconButton IconUri="/Images/appbar.save.rest.png" Text="save" Click="Save_Click"/>            
            
        </shell:ApplicationBar>
    </phone:PhoneApplicationPage.ApplicationBar>

</phone:PhoneApplicationPage>
